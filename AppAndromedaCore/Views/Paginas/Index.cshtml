@model IEnumerable<BAL.Modelos.Configuracion.PaginaModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<input type="hidden" name="Message" id="Message" value="@ViewBag.Message" />
<input type="hidden" name="AlertType" id="AlertType" value="@ViewBag.AlertType" />
<input type="hidden" name="ShowAlert" id="ShowAlert" value="@ViewBag.ShowAlert" />
<input type="hidden" name="ShowMsg" id="ShowMsg" value="@ViewBag.ShowMsg" />


<div class="d-sm-flex align-items-center justify-content-between mg-b-20 mg-lg-b-25 mg-xl-b-30">
    <div>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb breadcrumb-style1 mg-b-10">
                <li class="breadcrumb-item"><a href="#">Inicio</a></li>
                <li class="breadcrumb-item"><a href="#">Configuración</a></li>
                <li class="breadcrumb-item active" aria-current="page">Páginas</li>
            </ol>
        </nav>
    </div>
</div>

<div class="content-body">
    <div class="card bg-light mb-3" style="max-width: 100%;">
        <div class="card-header bg-gradient-gray" style="text-align:center">
            <div class="container">
                <div class="row">
                    <div class="col">
                        <a href="@Url.Action("Create","Paginas")" class="btn btn-success tmn1">
                            <i class="fa fa-plus-circle"></i>&nbsp;Agregar
                        </a>
                    </div>
                    <div class="col-6">
                        LISTA DE PAGINAS
                    </div>
                    <div class="col">
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body">
            <div class="col">
                <table id="ListaDatos" class="table table-striped table-hover table-responsive " width="100%">
                    <thead class="blue-dark">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Mensaje)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Accion)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Controlador)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Estado)
                            </th>
                            <th class="operaciones4">Operaciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Mensaje)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Accion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Controlador)
                            </td>
                            <td>
                                @*@Html.DisplayFor(modelItem => item.estado)*@
                                @if (item.Estado.ToString().Equals("1"))
                                {
                                    <span>Activo</span>
                                }
                                else
                                {
                                    <span>Inactivo</span>
                                }
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "Paginas", new { @id = @item.Id })" class="btn btn-info btn-xs tmn" data-toggle="tooltip" title="Editar">
                                    <i class='fas fa-pencil-alt'></i>
                                </a>
                                <a href="@Url.Action("Delete", "Paginas", new { @id = @item.Id })" class="btn btn-danger btn-xs tmn" data-toggle="tooltip" title="Eliminar">
                                    <i class='fa fa-trash'></i>
                                </a>
                            </td>
                        </tr>
                        }
                    </tbody>
                    <tfoot class="blue-dark">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Mensaje)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Accion)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Controlador)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Estado)
                            </th>
                            <th class="operaciones4">Operaciones</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>


<!-- page script -->
@section scripts{
    <script>
        $(function () {

            $('#ListaDatos').DataTable({
                "paging": true,
                "lengthChange": true,
                "searching": true,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>
}


@*<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mensaje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Accion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Controlador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mensaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Accion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Controlador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
